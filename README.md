# 📅 Планировщик занятий | Finance App

Современное веб-приложение для планирования занятий и активностей с красивым адаптивным дизайном и продвинутым функционалом.

## ✨ Особенности

### 🎨 Современный дизайн
- **Темная/светлая тема** - переключение одним кликом
- **Адаптивный интерфейс** - идеально работает на всех устройствах
- **Современная типографика** - шрифт Inter для лучшей читаемости
- **Анимации и переходы** - плавные интерактивные элементы
- **Font Awesome иконки** - красивые векторные иконки

### 📱 Адаптивность
- **Мобильные устройства** (320px+) - оптимизированный интерфейс
- **Планшеты** (768px+) - удобное управление сенсорным вводом
- **Десктоп** (1200px+) - полнофункциональный интерфейс
- **Высокий контраст** - поддержка для пользователей с ограничениями зрения
- **Уменьшенная анимация** - уважение к настройкам доступности

### ⚡ Функциональность
- **Toast уведомления** - красивые информативные сообщения
- **Валидация форм** - проверка данных и конфликтов времени
- **Экспорт в CSV** - сохранение расписания в файл
- **Автосохранение** - данные формы сохраняются автоматически
- **Горячие клавиши**:
  - `Ctrl+R` - обновить расписание
  - `Ctrl+E` - экспорт в CSV
  - `Ctrl+D` - переключить тему
- **PWA поддержка** - можно установить как приложение
- **Офлайн режим** - работает без интернета

### 🔧 Техническое совершенство
- **Service Worker** - кэширование для быстрой загрузки
- **CSS переменные** - легкая кастомизация тем
- **Семантическая разметка** - доступность и SEO
- **Оптимизированная производительность** - минимальные перерисовки
- **Прогрессивное улучшение** - работает даже с отключенным JS

## 🚀 Установка и запуск

### Локальная разработка

```bash
# Клонирование репозитория
git clone https://github.com/Pavel-Shcherbo/finance-app.git
cd finance-app

# Установка зависимостей
npm install

# Запуск сервера разработки
npm start
```

Приложение будет доступно по адресу `http://localhost:3000`

### Развертывание

#### TimeWeb Cloud Apps
1. Создайте новое приложение в панели TimeWeb
2. Выберите тип: **Backend → Express (Node.js 22)**
3. Подключите репозиторий: `Pavel-Shcherbo/finance-app`
4. Настройте переменные окружения:
   - `NODE_ENV=production`
   - `PORT=3000`
5. Команды сборки:
   - Build: `npm install`
   - Start: `npm start`

#### Другие платформы
Приложение совместимо с любой платформой, поддерживающей Node.js:
- Heroku
- Vercel
- Railway
- Render
- DigitalOcean App Platform

## 📋 API Endpoints

| Метод | Endpoint | Описание |
|-------|----------|----------|
| `GET` | `/api/activities` | Получить все занятия |
| `POST` | `/api/activities` | Создать новое занятие |
| `DELETE` | `/api/activities/:id` | Удалить занятие |
| `GET` | `/health` | Проверка состояния сервера |

### Пример запроса

```javascript
// Создание нового занятия
const response = await fetch('/api/activities', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
        name: 'Йога',
        day: 'понедельник',
        time: '09:00',
        duration: 60
    })
});
```

## 🎯 Использование

### Добавление занятия
1. Заполните форму в верхней части страницы
2. Выберите название, день недели, время и длительность
3. Нажмите "Добавить занятие"
4. Занятие появится в таблице расписания

### Управление расписанием
- **Удаление**: нажмите кнопку с корзиной рядом с занятием
- **Обновление**: используйте кнопку "Обновить" или `Ctrl+R`
- **Экспорт**: нажмите "Экспорт" или `Ctrl+E` для сохранения в CSV

### Персонализация
- **Тема**: переключайтесь между светлой и темной темой
- **Размер**: интерфейс автоматически адаптируется под ваш экран
- **Доступность**: поддержка программ чтения с экрана

## 🛠 Технологии

### Frontend
- **HTML5** - семантическая разметка
- **CSS3** - современные стили с переменными
- **JavaScript ES6+** - нативный JS без фреймворков
- **Service Worker** - PWA и офлайн поддержка
- **Font Awesome** - векторные иконки
- **Google Fonts** - типографика Inter

### Backend
- **Node.js** - серверная среда выполнения
- **Express.js** - веб-фреймворк
- **JSON** - хранение данных в памяти
- **CORS** - поддержка кросс-доменных запросов

### Инструменты разработки
- **Git** - контроль версий
- **GitHub** - хостинг кода
- **npm** - управление пакетами
- **ESLint** - линтинг кода (опционально)

## 📱 PWA возможности

Приложение поддерживает установку как Progressive Web App:

1. **Установка на Android/iOS**:
   - Откройте в браузере
   - Нажмите "Добавить на главный экран"

2. **Установка на Desktop**:
   - Откройте в Chrome/Edge
   - Нажмите иконку установки в адресной строке

3. **Офлайн работа**:
   - Интерфейс кэшируется локально
   - Данные сохраняются в localStorage
   - Уведомления о статусе соединения

## 🎨 Кастомизация тем

Приложение использует CSS переменные для легкой кастомизации:

```css
:root {
    --primary-color: #6366f1;      /* Основной цвет */
    --bg-primary: #ffffff;         /* Фон */
    --text-primary: #1e293b;       /* Текст */
    --border-color: #e2e8f0;       /* Границы */
    --shadow-md: 0 4px 6px...;     /* Тени */
}

[data-theme="dark"] {
    --bg-primary: #0f172a;         /* Темный фон */
    --text-primary: #f1f5f9;       /* Светлый текст */
    /* ... другие переменные */
}
```

## 🔧 Настройка для разработки

### Структура проекта
```
finance-app/
├── server.js              # Express сервер
├── package.json           # Зависимости и скрипты
├── README.md             # Документация
└── public/               # Статические файлы
    ├── index.html        # Главная страница
    ├── style.css         # Стили
    ├── script.js         # JavaScript
    ├── sw.js            # Service Worker
    └── manifest.json     # PWA манифест
```

### Переменные окружения
```bash
NODE_ENV=development    # Режим разработки
PORT=3000              # Порт сервера
HOST=0.0.0.0          # Хост (для продакшена)
```

### Скрипты package.json
```json
{
    "scripts": {
        "start": "node server.js",
        "dev": "nodemon server.js",
        "test": "echo \"No tests specified\""
    }
}
```

## 🐛 Решение проблем

### Частые проблемы

1. **Приложение не загружается**
   - Проверьте, что сервер запущен на правильном порту
   - Убедитесь, что все файлы загружены

2. **API не работает**
   - Проверьте URL в переменной `API_BASE_URL`
   - Убедитесь, что сервер доступен

3. **Стили не применяются**
   - Проверьте загрузку CSS файла
   - Очистите кэш браузера

4. **PWA не устанавливается**
   - Проверьте, что приложение работает по HTTPS
   - Убедитесь, что manifest.json доступен

### Отладка
- Откройте Developer Tools (F12)
- Проверьте Console на ошибки
- Во вкладке Network смотрите статус запросов
- В Application проверьте Service Worker и localStorage

## 📞 Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте этот README
2. Посмотрите Issues на GitHub
3. Создайте новый Issue с описанием проблемы

## 📄 Лицензия

MIT License - используйте свободно для личных и коммерческих проектов.

## 🚀 Планы развития

- [ ] Интеграция с календарями (Google Calendar, Outlook)
- [ ] Уведомления о предстоящих занятиях
- [ ] Повторяющиеся занятия
- [ ] Цветовое кодирование по типам активности
- [ ] Статистика и аналитика
- [ ] Импорт из CSV/Excel
- [ ] Мультиязычность
- [ ] Синхронизация между устройствами

---

**Создано с ❤️ для эффективного планирования времени** 